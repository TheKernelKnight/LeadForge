{
  "name": "leadforge",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm -r dev",
    "build": "pnpm -r build",
    "start": "pnpm -r start",
    "db:push": "pnpm -F api db:push",
    "db:seed": "pnpm -F api db:seed"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
